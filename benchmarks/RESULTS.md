# RESULTS — measured, reproducible

Generated by `python benchmarks/make_results.py <sweep_dir>`. Every number
here comes out of that script; nothing is hand-typed. Re-run it and the
file regenerates identically from the same inputs.

**Historical provenance:** the input sweep JSONs record engine version(s) 0.3.0. This report renders those records; it does not run the current engine or refresh their measurement date. The original JSON metadata and adjudication dates remain unchanged.

## Human-history corpus (in-sample)

Human-authored, human-reviewed history from six active OSS Python
projects, 300 consecutive non-merge commits each. After the first
measurement on 2026-07-30, these same diffs were repeatedly reviewed
and used to tune the analyzer. The recorded result is in-sample,
not a held-out estimate for unseen repositories. The engine version the
records carry is stated above; it is the version that swept them.

```bash
checkwash sweep HEAD --limit 300 --repo <path>   # measurement command, not run by this generator
```

**Blocked (would fail CI at the default `fail_on = high`): 46/1800 = 2.56%** (v0.3.0 tracked sweep, 2026-09-07, runner pool, release commit 4387097)

| repo | commits | touching tests | blocked | rate |
|---|---:|---:|---:|---:|
| flask | 300 | 37 | 8 | 2.67% |
| httpx | 300 | 92 | 13 | 4.33% |
| attrs | 300 | 65 | 2 | 0.67% |
| click | 300 | 109 | 15 | 5.00% |
| rich | 300 | 71 | 6 | 2.00% |
| starlette | 300 | 92 | 2 | 0.67% |
| **total** | **1800** | **466** | **46** | **2.56%** |

Engine errors: 0.

**Recorded commits with opaque production changes: 24/1800 = 1.3%.** This is the stored `commits_with_opaque_prod_change` count. Opaque-change repair credit can keep oracle findings below the blocking threshold (THREATMODEL #4); the count alone does not show that the credit changed each verdict, or measure the current engine's analysis coverage.

Blocking findings by rule (commits containing at least one):

| rule | commits |
|---|---:|
| `TEST_DISABLED` | 16 |
| `ASSERT_WEAKENED` | 12 |
| `EXPECTED_VALUE_CHANGED` | 9 |
| `EXPECTATION_DEFINITION_CHANGED` | 7 |
| `ASSERT_SUBSTITUTED` | 2 |
| `EXPECTED_VALUE_HARDCODED` | 1 |
| `CI_WORKFLOW_TOUCHED` | 1 |
| `ASSERT_REMOVED` | 1 |

## Decomposed: what those blocks are

A block is not automatically a false positive. Every one of the
46 blocks above was adjudicated against the real diff, into
*false positive* (blocking was wrong), *spec-correct*
(the diff really does drop oracle coverage with no visible
compensation — the tool doing its documented job, allowlist it), or
*unclear*.

| measure | count | rate over 1800 commits |
|---|---:|---:|
| historical human-commit **block rate** | 46 | **2.56%** |
| adjudicated **false positive** | 31 | **1.72%** |
| **legitimate policy block** (spec-correct) | 15 | 0.83% |
| **unclear** | 0 | 0.00% |

The headline figure to compare against other tools is the block rate,
because that is what a team feels in CI. The figure that says whether
greenwash is *wrong* is the adjudicated false-positive rate.

Raw per-commit verdicts and reasoning: `adjudication-2026-09-07.json`.

Recorded adjudication date: **2026-09-07**. Source: tracked sweep benchmarks/sweeps/ (checkwash v0.3.0, release commit 4387097ad07994ef594dcc0c5f9e43d4475321e0, estate runner-pool run 34121457167, clean checkouts on Python 3.12), 6 OSS repos x 300 commits = 1800.

How they were judged: the 42 verdicts of adjudication-2026-08-26b.json are carried verbatim. The v0.3.0 sweep blocks 46 of the same 1800 commits (gone 0, new 4). The four new blocks were each judged by the maintainer in a single pass against the real diff, on the operator's written reading recorded in estate T-201 (Nelson, 2026-09-07: 同意) and, for click 61bdc2ae81, the ruling recorded in estate T-171 §6 (Nelson, 2026-09-07: accepted as cost) - weaker independence than the blind-rater cohorts, noted per the 2026-08-26 precedent. All four are false_positive; three of them (click c498ced0, flask 0a00e1b6, httpx 7985f685) have blocked since v0.2.12's reference arm and were first named in issue #132 §4; click 61bdc2ae81 became a block with the v0.2.14-line parametrize row-identity change (#135)..

**How much to trust the split.** The block rate is a machine count and
is exact; the split between *false positive* and *legitimate policy
block* is a judgement call on 46 diffs, and the boundary is genuinely
arguable on some of them (a test deleted because its coverage moved
upstream is invisible to any diff analyser — called spec-correct here,
another reviewer might say the tool should not have blocked). Read the
per-commit reasoning and disagree; the file exists so you can.

An earlier **35-diff cohort** was judged 3 times. rater A = original per-commit adjudication; raters B and C = independent blind passes over the same 35 diffs, with no access to prior verdicts
The following agreement statistics describe only that cohort, not all 46 blocks in this later snapshot. The two blind-pass files are `adjudication-rater-B-2026-08-04.json` and `adjudication-rater-C-2026-08-04.json`.

- pairwise agreement: A-B 94.3%, A-C 91.4%, B-C 91.4%
- Cohen's kappa: A-B 0.88, A-C 0.83, B-C 0.82
- Fleiss' kappa across all 3: **0.844**
- commits with any disagreement: 4 of 35
- reconciliation: published category = majority of the three; a 3-way split would become unclear (none occurred)

The 5 blocks the 2026-08-25 promotion added were judged separately by 2 blind raters each (two blind raters per commit with distinct lenses; 1-1 splits settled by a diff-reading reconciler; dissents preserved); 2 of 5 drew a split. the written line (docs/defence-design.md, ~5 of 1800) permits at most five new blocks judged false; five new blocks means the line cannot arithmetically fail this round, so the adjudication is reported as evidence, not as a gate that discriminated. Incremental precision of the promotion on honest history: 0/5.

Later adjudication files carry earlier verdicts forward and may remove
previously blocked commits. Additional judgments, including maintainer
single-pass review, are identified in their method and per-commit fields;
do not treat the historical split as one uniformly three-rater cohort.

## Where the earlier rounds' blocks came from

Every oracle-rule block from the first sweep round was triaged by an
independent agent reading the real diff, classifying each as
*spec_correct* (a genuine uncompensated oracle deletion — the tool doing
its job, allowlist it) or *fixable_fp* (the diff compensates in a way a
deterministic analyser could see).

- triaged: **48** commits
- fixable_fp: **34**
- spec_correct: **14**

Raw verdicts with per-commit reasoning: `triage-2026-07-30.json`.
The 34 fixable FPs drove two precision rounds; their mechanisms are now
regression fixtures in `tests/cases/`.

## Honest limits of this number

- **Historical and in-sample.** Regenerating this page neither runs the
  current release nor creates a new held-out result. Keep the recorded
  engine version, corpus, date, and adjudication attached to the rates.
- **It is a block rate, not a false-positive rate.** Some of these blocks
  are correct: the commit really did drop oracle coverage with nothing
  visible replacing it, and a reviewer would allowlist it. The decomposed
  numbers are above; only the *adjudicated false positive* line is a
  false-positive rate.
- It measures **behaviour on human history**, not recall against agent
  cheating. Recall is measured separately on the decoy corpus
  (`decoy/README.md`): 12/12 blocked, and 0/12 false blocks on the honest
  arm.
- The gate is about *blocking*. Non-blocking `warn` findings are more
  frequent by design — they are visible, not gating.
- Six repos, all mature and well-reviewed. A younger codebase with looser
  test hygiene may differ.
- The head-to-head against swarm-orchestrator is in `compare/`, with its
  caveats stated there.
