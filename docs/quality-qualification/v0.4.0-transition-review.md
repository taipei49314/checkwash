# v0.4.0 legacy transition review, candidate snapshot

This is a source review of candidate `62504420447e20117d3a4624123b79ac06f14d96`.
It is **not** approval to replace the historical byte-comparison gate, and
does not qualify a later source revision automatically. No release record is
enabled in the workflow.

The immutable comparison source is
`387e71befedc387ab0c85af4c3322b7877a0cfd1` (v0.3.3). The retained emitted
baseline SHA256 is
`67d2fcfd2550c06ebfb2b6b18b39844b903e8b521344f820e864480f34f620f5`;
candidate output SHA256 is
`ea6d6db1b977368120e74ff3b3e0140d0b3a7d7539c8cff32ed7727608c34f8a`.
The original result remains `byte_identical: false`, exit 1.

The complete parsed inventory preserves all 377 original cases and adds one
case, `param_row_answer_replaced_pos.gwcase`, introduced by the #156 fix.
Every case has exactly one findings object and one IR object. The comparison
found no changed original-case verdict, rule or severity.

| Exact delta category | Count | Review |
|---|---:|---|
| Version field | 377 | `run.checkwash_version` reports the new source version. |
| New IR fields | 2734 | Explicit evidence fields for module constant renames, input identities, expectation provenance and subject installations/shadows; all original fields remain accounted for. |
| Other existing-field changes | 67 | The three cases detailed below; none changes its verdict, rule or severity. |
| New case documents | 2 | Findings and IR for the one additional regression case. |

`js_test_skipped_pos.gwcase` has seven changes. Its real name, `applies tax`,
replaces `anonymous_0`; the disabled-marker span covers the named declaration,
and pairing becomes `by_name`. The message, unit and content-bound fingerprint
follow the corrected identity. `TEST_DISABLED/high` remains the finding.
Consumers matching stored fingerprints should review this identity correction
instead of treating the prior anonymous name as stable.

`oracle_helper_renamed_neg.gwcase` has 27 IR changes, and
`oracle_moved_into_helper_neg.gwcase` has 33. Closed table/helper projection
now represents the preserved concrete production calls, literal arguments
and expected answers directly. Synthetic unit identities, reaching names,
source spans, body hashes, inherited flags and assertion-pair metadata follow
that representation. The moved-helper case no longer records a logic change
after equivalent concrete oracles are established. Both remain silent/pass.

The comparison tool now records every typed JSON leaf/addition/removal and
rejects dropped/duplicated cases or malformed artifacts. An independently
reviewed transition record must also bind the complete source/corpus hashes,
a tracked review document and a rationale for every exact delta. Even a
matching record produces only a separate transition qualification; it never
changes the raw byte-comparison exit status. Later detector work, protected
fixture decisions and final release metadata require a fresh comparison.
