# v0.4.0 candidate and release record

**Status: preparation and qualification in progress; not yet published.**
The user authorized this version under estate T-436. The integration is
[PR #160](https://github.com/taipei49314/checkwash/pull/160).

This round covers the 17 open issues inventoried from the repository:
84–93, 95, 130, 132, 140, 156, 158 and 159. An issue remains open until
its reported behavior is resolved and verified; this list is scope, not a
claim that every issue has been closed.

The candidate adds bounded proofs for pytest execution/report hooks,
collection settings, fixture subject replacement and import-path providers;
expectation provenance, inherited test methods, checked doctest examples and
literal branch outcomes; and additional transparent table refactors. The
JavaScript scanner distinguishes real test declarations from comments,
strings and regular expressions, and duplicate test names retain their
individual multiplicity during alignment.

## Qualification status

- Source regression tests and live pytest reproductions are being integrated.
- Issue #93 requires the input-change classification decision described in
  [the review proposal](../issue-93-input-contract-review.md).
- Historical tamper case `035-identity` now blocks because removal of an
  inherited collected test is visible. Its frozen expectation update awaits
  maintainer classification; the original gate still reports that mismatch.
- The old quality comparison remains a raw comparison to v0.3.3. A separate
  exact transition report is being prepared; no historical byte equality is
  claimed and no unreviewed transition has been activated.
- Intermediate 178-case CLI replay counts from the first local materializer
  were withdrawn after a Git stat-cache defect was found. The corrected
  materializer verifies every committed file and blob against each recorded
  side. The complete verified `a6d3368` replay blocks 47/88 captured escapes
  and passes 75/90 historical false-positive-folder cases, versus 15/88 and
  28/90 on verified v0.3.4. These are engine verdicts on historical labels;
  all 178 snapshot pairs match the reference bytes. There are no verdict
  regressions against either v0.3.4 or the preceding `f549ed3` replay.
  Later repairs still need another full replay.
- All 75 preserving cases in that 90-case population now have runtime controls:
  captured production passes both suites, and an independent wrong-output
  implementation produces completed assertion failures in both. The inventory
  verifies all 900 captured snapshot files against the immutable corpus commit.
  The other 15 cases retain their historical labels separately: 14 lose a
  completed failure, while case 051 loses a fixture setup failure.
- The separate 2026-09-02 stress population has now been located locally and
  captured without modifying its original checkout: 5,385 files, 1,810,471 bytes,
  SHA256 `9f69b50922bf04b49bd30cfdaf0710b5bb14994915b78fa5b135dc527d5716f4`.
  These records are untracked; their containing Git HEAD is not their identity.
  There are 300 saved escape samples and 81 saved historical false positives.
  Fresh v0.3.4/candidate replays are underway. The 306 capped unsaved families
  remain unavailable and are not counted as fixed or replayed.
- The v0.3.4 six-repository baseline analyzed the same fixed 1,800 commits:
  46 blocks, no engine errors. Candidate `f549ed3` has the same totals.
  All 1,800 public verdicts and complete finding/decision records were compared:
  1,799 are identical. One httpx pass-to-pass commit adds five expected-provenance
  warnings after an accept-encoding expectation adds zstd; existing findings
  and severities remain unchanged. No block is added or removed. This comparison
  does not claim full IR equality, and the final-source sweep remains required.
- The nine OS/Python corpus artifacts for candidate `5fe30ca` agree exactly
  (`ea6d6db1b977368120e74ff3b3e0140d0b3a7d7539c8cff32ed7727608c34f8a`).
  [CI run 35536623217](https://github.com/taipei49314/checkwash/actions/runs/35536623217)
  retains failed test results: two checks of the same frozen tamper record
  and the missing-tag check on every leg. Ubuntu 3.12 now passes the performance
  gate, but Ubuntu 3.13 fails: median 2.51 seconds against unchanged 2.5 seconds.
  The local immutable `14410b1` run has 3,893 passed, one Windows symlink skip
  and the three frozen-record/tag failures. The performance failure is real
  outstanding qualification work; no threshold or test has been changed.
  Passing corpus determinism does not turn those tests into successes.
- A further performance repair in `c68c107` reuses per-unit scope discovery and
  defers unused context/vacuity walks. Five interleaved local measurements
  reduce the median of original-gate medians from 1.115 to 1.007 seconds;
  host-load variation remains a limitation. All 378 corpus findings/full IR
  and 24 nested-helper/context-manager edge records remain byte-identical.
  A fresh public matrix is required; no threshold or output was weakened.
- Windows CPython 3.11 packaging on immutable `f549ed3` passed fresh offline
  wheel installation, both CLI aliases, demo/doctor, clean/block/error exit
  codes and zipapp output contracts. Wheel and zipapp package contents, and
  sdist public surfaces, match that Git source. The artifact digests are:

  | Artifact | SHA256 |
  |---|---|
  | wheel | `22c62209f3a768015255de9e2fbe83453f515bd4b43be7890247b622e02af722` |
  | sdist | `cedb8a7ff33a12b0d4217e26ceadcdcb168b3e07240eb2f6c0c957e296d5ea92` |
  | zipapp | `0554a798979711169325a828130cf1fdc12d9f75d080584d79b2ddabae97e9ba` |

Final-source tests, the nine OS/Python comparison, exact tag/source identity,
package checks and release/PyPI asset identity remain publication conditions.
The historical v0.3.4 release and all archived measurements stay unchanged.

## Try it on a change you understand

After publication, use Python 3.11+ and Git:

```sh
pipx install checkwash==0.4.0
checkwash --version
checkwash demo
checkwash check HEAD~1..HEAD --format json
```

The recommended Action retains the one-release trust lag: v0.3.4, commit
`23fce200561ae4a95e46539ed9e668088591d1c1`. It does not include the new
v0.4.0 detector and precision changes. Record the CLI and Action versions
separately. Alpha status, remaining bounded-model gaps and the distinction
between an engine replay and runtime label qualification remain explicit.
