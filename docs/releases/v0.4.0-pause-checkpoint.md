# v0.4.0 pause checkpoint

Work stopped at the user's request on 2026-09-21. PR #160 remains a draft.
The published release is still v0.3.4. No issues were closed, and no merge,
release tag, GitHub Release or PyPI publication was performed.

## Saved integration

The last detector/test integration is `90f4c097dae54db15b59ec625e339f41ae2ff24b`.
It adds reviewed repairs for complete unique-helper answers, reserved helper
bindings, constant builtin normalization and generator-fixture subject
replacement after the last fully exercised CI checkpoint `31a8249`.
The integrated source collects 6,935 tests. Its last combined targeted run
passed 256 tests; this is not a full qualification of the final integration.

Two completed candidate commits remain outside the integration pending
independent review:

| Commit | Scope | Author-side validation |
|---|---|---|
| `26eae068b1ee8f53d5752af7676abc0f22635977` | Static case 002, closed literal class guards | 343 focused checks; original/honest runtime controls |
| `486eb1db492037411b942a3d365ecb5d9c37bedc` | LLM cases 018/064/072, set comparison carriers | 344 focused checks; original/honest runtime controls |

## Completed public CI

[Run 35546543504](https://github.com/taipei49314/checkwash/actions/runs/35546543504)
tests `31a8249dc0ad82c4d74a28f1601274cf7f35e7d4`: all nine OS/Python legs
report 6,676 passes and three failures. Two failures consume the same frozen
tamper `035-identity` expectation; the third requires the absent v0.4.0 tag.
All performance gates pass. The nine raw corpus payloads are byte-identical
(2,748,393 bytes; SHA256
`ea6d6db1b977368120e74ff3b3e0140d0b3a7d7539c8cff32ed7727608c34f8a`),
and all ten downloaded artifact ZIPs match their GitHub API hashes.

The preceding `2ed8591` run additionally failed the documentation test count
check. `31a8249` fixes that documentation regression; the original failed
result is retained. The immutable local `2ed8591` run records 6,674 passes,
one Windows symlink skip and four failures. Neither run qualifies later
source integrations automatically.

## Outstanding work

- Resolve the input-change contract for #93 and the exact frozen035 label
  decision. The proposals are in `docs/issue-93-input-contract-review.md` and
  the local release review record; protected contracts and labels are unchanged.
- Complete independent reviews of the two saved candidate commits before
  integrating them; finish the remaining reported/captured defects. All
  17 scoped issues remain open: #84–#93, #95, #130, #132, #140, #156, #158, #159.
- Preserve full historical replay results with their exact source identities.
  A historical false-positive folder label is not evidence of preservation:
  case 014 exposed a real identity-loss regression, repaired in `5881943`.
- The 300 static escape runtime pairs have only a source/INI safety audit.
  No new runtime harness was implemented, and 0/300 pairs were executed in
  that proposed qualification. The separate completed 81-case runtime
  classification remains valid for its exact snapshots.
- Repeat final-source full tests, both historical populations, the fixed
  1,800-commit sweep and wheel/sdist/zipapp qualification; verify public
  matrix parity, final source/tag identity and the release workflow.
- The legacy v0.3.3 raw quality comparison is still red. No waiver or
  transition acceptance was activated. Do not treat it as a passing check.

This is a resumable checkpoint, not completion of the issue or release scope.
