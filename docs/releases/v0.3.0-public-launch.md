# checkwash v0.3.0: content-bound exemptions, row identity, first use

**Alpha. The [1.0 criteria](../stability.md#what-must-change-before-10) are not met.**

This release changes detector logic. Five file-wide exemption rules move to
content-bound identities and their path-only keys are retired; parametrize
rows are compared by identity instead of by count; table-driven oracles that
delegate to a same-file helper or a literal-table fixture are read per row;
the findings and IR schemas advance to 2. The recommended Action pin advances
to the prior release, v0.2.13, under the one-release trust lag. The
six-repository sweep was re-run on the release commit on 2026-09-07 and is
now the tracked benchmark record (PR #141).

## What changed since v0.2.13

- **Reviewed exemptions bind to content.** `GUARDRAIL_TOUCHED`,
  `CI_WORKFLOW_TOUCHED`, `TEST_FILE_UNPARSEABLE`, `SCOPE_DRIFT` and
  `SNAPSHOT_CODE_COCHANGE` used a fingerprint derived from the rule and path
  alone, so one recorded approval could cover a later change to the same
  path. Their keys now end in `/v2:` plus a SHA-256 over both analyzed
  snapshots and the rule-specific context; legacy keys stay visible but cannot
  suppress a finding. There is no automatic upgrade. See the
  [upgrade notes](../remediation-upgrade.md).
- **Parametrize rows have an identity.** `TEST_DISABLED` counts disabled rows
  by their cell text (deleted rows still by count), so appending a new row no
  longer hides a disabled one; `EXPECTATION_DEFINITION_CHANGED` keys expectations by their
  input columns and reads every cell of a `pytest.param(...)` row
  ([#134](https://github.com/taipei49314/checkwash/pull/134),
  [#135](https://github.com/taipei49314/checkwash/pull/135)).
- **Table-driven oracles that delegate are read per row.** A same-file helper
  that is one bare `assert` with no message, and a zero-argument literal-table
  fixture, are inlined per row before the oracle is judged; anything else
  falls back to the general frontend
  ([#136](https://github.com/taipei49314/checkwash/pull/136)).
- **Conftest stand-ins and stored expectations** are resolved from strict
  snapshots, including imported `unittest.mock` forms
  ([#127](https://github.com/taipei49314/checkwash/issues/127),
  [#128](https://github.com/taipei49314/checkwash/issues/128),
  [#129](https://github.com/taipei49314/checkwash/issues/129)).
- **Installation and output fixes:** local hook installation and `doctor`
  agree on the runtime they bind; human diagnostics survive legacy Windows
  pipes; SARIF locations point at the analyzed source; workflow diagnostics
  explain rejected configurations; numeric-restoration and root-helper
  precision are bounded more narrowly.
- **Cross-interpreter determinism.** The release CI's byte-compare gate (every
  OS and Python version must emit byte-identical corpus findings, SPEC §8)
  ran for the first time since v0.2.13 and failed on Python 3.13: `ast.dump`
  omits empty fields by default there, and two places digested that text.
  Both now go through `stable_dump`, which pins the pre-3.13 format, so keys
  computed on 3.11 and 3.12 are unchanged and 3.13 agrees with them. The gate
  is green on the released commit for all nine OS/Python combinations.

The full commit list is in the
[GitHub Release](https://github.com/taipei49314/checkwash/releases/tag/v0.3.0)
and the dated section of [`STATE.md`](../../STATE.md).

## Get this version

| Surface | Version and source |
|---|---|
| Python package | [PyPI checkwash 0.3.2](https://pypi.org/project/checkwash/0.3.2/) (the v0.3.0 engine; 0.3.0 and 0.3.1 stay on PyPI) |
| Single-file CLI | [GitHub Release v0.3.2](https://github.com/taipei49314/checkwash/releases/tag/v0.3.2) |
| Source | [Tag v0.3.2](https://github.com/taipei49314/checkwash/tree/v0.3.2) (engine as at [v0.3.0](https://github.com/taipei49314/checkwash/tree/v0.3.0)) |
| Recommended Action | v0.3.1, commit `3a4cfcd`-tagged source (`git rev-parse 'v0.3.1^{commit}'`), following the one-release trust lag (advanced by v0.3.2); the same v0.3.0 engine |

The CLI, zipapp and Action are separately versioned dependencies. Record the
surface and version actually deployed. **v0.3.1 and v0.3.2 (2026-09-07)** are
documentation and metadata releases on the v0.3.0 engine, cut so that the PyPI
description matches this repository (D-057, D-058); v0.3.1 advanced the
recommended Action pin to v0.3.0, which carries this release's detector
changes, and v0.3.2 advanced it to v0.3.1, the same engine.

### Release asset digests (SHA-256)

| Asset | Size | SHA-256 |
|---|---:|---|
| `checkwash-0.3.0-py3-none-any.whl` | 243,460 | `a23b2580ba10980b69a3c3eef07ae5db1597bed319a45914685306551db20382` |
| `checkwash-0.3.0.tar.gz` | 919,178 | `af2eda282f9291f28dc1346eb5c7ff5c52c2e93a47496330017ece2736ae6da8` |
| `checkwash.pyz` | 232,346 | `51b4cc86cef3b50b3ebf47fbf2fa4006b9861ef46639b84755eea27b86354af5` |
| `checkwash-0.3.1-py3-none-any.whl` (v0.3.1) | 243,502 | `8389138525866644c42c702aaae4f51ac4e3c46b007317c6ce590daf2bfc903a` |
| `checkwash-0.3.1.tar.gz` (v0.3.1) | 950,468 | `e61eec94c7e4cb0cb8a7608f11bb833026fc72790c8033293f9c27764515eee1` |
| `checkwash.pyz` (v0.3.1) | 232,345 | `8fd7181effe05e9ef14532aafc646289caa64a71d16c269bb948dd9520a648f7` |
| `checkwash-0.3.2-py3-none-any.whl` (v0.3.2) | 243,513 | `3b8c16d7cdb39b60d1bb28ee93ef80a301c8b182bb0a8f60249303b41cef2363` |
| `checkwash-0.3.2.tar.gz` (v0.3.2) | 951,956 | `3f4f298bfde04c4abe9ac779f60b411fce660b9578482b094d16d0c78e9dbc5b` |
| `checkwash.pyz` (v0.3.2) | 232,343 | `b503d29fc916691136a2a63e0a762844e22930c57fc51e211c2ebdb4b34e8816` |

The wheel and sdist on PyPI carry the same digests as the same-named GitHub
assets (checked on 2026-09-07 by downloading both sides). The zipapp is a
GitHub asset only.

## Try it on a change you understand

Use Python 3.11+ and Git. In a disposable virtual environment:

```bash
python -m pip install checkwash==0.3.0
checkwash --version
checkwash check HEAD~1..HEAD
checkwash check HEAD~1..HEAD --format json
checkwash check HEAD~1..HEAD --format sarif
```

Or with the single file:

```bash
curl -LO https://github.com/taipei49314/checkwash/releases/download/v0.3.0/checkwash.pyz
python checkwash.pyz --version
python checkwash.pyz demo
```

Run these inside your repository with two available commits. For a PR, use
`checkwash check BASE...HEAD` to compare the merge base with HEAD. Keep
normal tests and human review: a pass means no configured blocking finding
was found, not that the change is correct or honest.

Exit codes are **0 pass / 1 block / 2 engine error**. Capture the exit code
and stderr as well as stdout. The
[machine-interface contract](../stability.md#machine-findings-contract-findings_version--ir_version)
describes the schema-2 keys and the compatibility exception.

To stop a merge, install the [canonical workflow](../../README.md#required-check--the-only-configuration-that-blocks-a-merge)
and make its `checkwash` status context required. `checkwash doctor` inspects
local configuration; it cannot confirm live branch protection. See
[enterprise setup](../enterprise.md) for exemptions and configuration protection.

## If you recorded exemptions on v0.2.13

Exemptions for the five rules above are ignored by v0.3.0 and reported on
stderr as retired keys; `checkwash allow` rejects new legacy keys and
`doctor` counts retired and invalid entries separately. Re-review each change
and record a new content-bound key; the old reason does not contain the
content pair needed to reconstruct the approval. The
[upgrade notes](../remediation-upgrade.md) list the exact steps and the
runtime limits.

## Verify the download and publication

Download the wheel, sdist or `checkwash.pyz` from the versioned release above
and compare its complete SHA-256 with the table in this document and with the
release asset digest. Run `checkwash --version` or
`python checkwash.pyz --version` on the surface you installed. A file's
existence alone does not establish a successful run.

The versioned Release page records the publishing workflow and available
assets. The [release process](../RELEASING.md#check-the-public-package-page)
requires metadata, rendered links and downloaded-byte checks before reporting
publication complete; those checks were run for this version.

## Evidence and remaining work

The authoritative numbers in [`STATE.md`](../../STATE.md) were re-measured
for this release on 2026-09-07 (PR #141): the six-repository sweep records
engine **0.3.0** (the release commit, swept on the estate runner pool, run
34121457167) and its adjudication is dated 2026-09-07 (46/1800 blocked,
31/1800 adjudicated false positive, 15/1800 legitimate policy block,
24/1800 recorded opaque production changes, 12/12 classic decoys
blocked). They are in-sample results on a corpus that tuned the detectors.
**This release changed detector behaviour**, and the
[release process](../RELEASING.md#after-the-release) says the corpus sweep is
not optional in that case; the four blocks new since the v0.1.46 record were
judged false positive in a maintainer single pass.

The current-state issues filed against v0.2.13 remain open and describe the
population this release aims at:
[#132](https://github.com/taipei49314/checkwash/issues/132) (LLM-arm
re-judge: 85 of 88 recorded escape families still passed, 90 of 90 recorded
honest refactors still blocked, on v0.2.13) and
[#130](https://github.com/taipei49314/checkwash/issues/130) (60 of those 90
are consolidations of N named tests into one parametrize, fixture or loop,
read as `TEST_DISABLED`/high). The parametrize-row and table-oracle changes in
this release are the first cut at that family. Re-judged on v0.3.0 through
the CLI path on 2026-09-07 (estate runner pool, 178 families): 79 of 88
escape families still pass and 65 of 90 honest refactors still block;
the per-family results are posted on #132 and #130.

**Maintainer pass (2026-09-07, D-056).** `THREATMODEL.md` and `SPEC.md` carry
the rows for the parametrize-row identity and table-oracle delegation
changes (the 86a cost line for the new adjudicated-false block, the new
skip-mark rows 102 and 102a, the delegated-oracle source and its
`python_functions` dependency in SPEC §2b, the `TEST_DISABLED` /
`EXPECTATION_DEFINITION_CHANGED` wording), row 101 is closed and pinned
end-to-end, and the `STATE.md` narrative for the 0.3.0 section is written.
The pass was done by the operator under the maintainer's explicit
authorization and is reviewed like any other change.

The [September catalog](../adversarial-catalog-2026-09.md),
[measurement sources](../../benchmarks/README.md) and
[stability limits](../stability.md#coverage-and-adoption-cost) describe known
limitations. Independent adoption observations and agreed refactor-cost
thresholds are still required before a 1.0 decision.

For ordinary feedback, report the version, surface, OS/Python, exit code and
what you expected in [issues](https://github.com/taipei49314/checkwash/issues).
Use [SECURITY.md](../../SECURITY.md) for security reports.
