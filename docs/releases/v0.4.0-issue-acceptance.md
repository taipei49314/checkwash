# v0.4.0 issue acceptance inventory

This is an in-progress source acceptance record for PR #160. The 17 issues
were open on main `568a97166bd56539fd1a76188f77ad7f3f514c63` when the user
requested their resolution and selected v0.4.0. A row with regression evidence
does not mean the source is merged or the issue is closed.

| Issue | Candidate behavior and concrete evidence | Remaining condition |
|---|---|---|
| #84 | Runtime/report hooks that suppress execution or replace pytest outcomes are recognized; `test_issue84_runtime_hooks.py` includes actual red-to-green runs. | Integrated release qualification. |
| #85 | `setattr(request.module, ...)` subject installation is exercised across fixture/hook/module forms by `test_issue_runtime_acceptance.py`. | Integrated release qualification. |
| #86 | Namespace and extended-package provider shadowing is checked by `test_issue_shadow_acceptance.py`. | Integrated release qualification. |
| #87 | A complete ten-occurrence module constant rename stays silent; `test_issue_87_constant_rename.py` checks unchanged correct/buggy runtime behavior. | Integrated release qualification. |
| #88 | Local assignment and fixture-parameter stand-ins are visible to subject installation; fixture and alias controls remain explicit. | Integrated release qualification. |
| #89 | Local/helper/fixture/table expected values keep provenance through supported literal bindings; `test_issue_expectation_families.py` exercises the six reported variants and local hops. Further historical table forms are being checked. | Complete remaining population review. |
| #90 | Selected root pytest settings and existing-option changes retain collection-narrowing evidence; `test_issue90_collection_matrix.py` covers precedence, relocation, overrides and live runs. | Integrated release qualification. |
| #91 | Plain conftest assignments are covered across seven installation spellings and five execution scopes in root/nested paths. | Integrated release qualification. |
| #92 | Existing helper arguments and module-table expected values cannot hide rewrites; named tests retain frozen-fixture and unrelated-expression controls. | Integrated release qualification. |
| #93 | The current contract deliberately leaves input-only changes silent. A concrete default-rule proposal identifies three contradictory frozen labels. | Human classification remains unanswered; no dependent rule or label edit has been made. |
| #95 | Provider inventory includes conftest search-path discovery and root/src configuration changes; `test_issue_shadow_acceptance.py` covers the reported provider matrix. | Integrated release qualification. |
| #130 | All 75 runtime-qualified preserving cases in the historical 90-case population pass at `a6d3368`; the other 15 have explicit lost-check witnesses. Transparent helpers, tuple components, callable fixtures, table factories, filters and shared collection inputs retain their complete obligations. | Complete integrated release qualification; historical labels and the original report's unavailable exact 60-case membership remain distinct. |
| #132 | The eight candidate families have bounded implementations or existing coverage: subject stand-ins, expected provenance, normalization, collection controls, literal branch guards, checked doctests and inherited tests. Historical population review continues. | Remaining captured escapes remain real tracked work; do not close from a partial count. |
| #140 | `benchmarks/refactors/README.md` explicitly identifies `results-latest.json` as the retained v0.3.1 dated snapshot and separates current expected records. Original results are unchanged. | Integrated documentation delivery. |
| #156 | PR #157's JavaScript identifier boundary fix is included; newline-like identifier suffixes cannot mint declarations. | Deliver through the integrated PR and supersede PR #157 accurately. |
| #158 | Same-named units pair as a multiset: identical bodies first, then remaining occurrences; duplicate deletion and weakening remain detectable. | Integrated release qualification. |
| #159 | Skip declarations retain their real names. Comment/string/regex text is excluded, with division and escaped-string controls. | Integrated release qualification. |

The source review also found and corrected false execution proofs involving
import rebinding in branch guards, replaced doctest owners, inherited property
methods and capture-time local result aliases. These review findings are part
of qualification, not evidence that an unchecked candidate was ready to ship.

## Evidence populations are distinct

The corrected CLI materializer verifies every committed file against the
immutable record `6466e616e3e39f221ec25910736b2ee1c6bfebc5`. On source snapshot
`80f9c50`, it records 61/88 captured escapes
blocked and 75/90 historical false-positive-folder cases passing; v0.3.4 has
15/88 and 28/90 respectively. These are engine replays, not new human labels.
All 178 cases retain identical before/after snapshot bytes across these runs;
there are no verdict regressions against v0.3.4 or the preceding `f549ed3` run.
Later source repairs need a fresh final replay. Earlier materializer receipts
were withdrawn because they did not verify the committed snapshot bytes.

Fifteen entries in the historical false-positive folder have independent
counterexamples showing lost checks (identity weakened to equality/truthiness,
removed inputs or self-derived expectations). The original folder labels
remain historical; unblocking all 90 entries would be an incorrect objective.
Four-run controls for repaired preserving cases check both suites on captured
good production and an independent wrong-production control. All 75 preserving
cases now qualify: 39 hash-bound existing receipts and 36 newly run cases
(144 pytest executions). All 900 captured snapshot files match the immutable
record. Case 066 additionally has one fixture setup failure alongside four
completed failures in its edited wrong-production control; that detail is
retained. Of the 15 loss cases, 14 are completed failures and 051 is a fixture
setup failure, so those observations are not conflated.

The separate local 2026-09-02 stress input has been found and captured with
all 5,385 files verified before and after copying. Full verified replays on
the same 381 snapshots record 43/300 captured escapes blocked and 7/81
historical false-positive-folder cases passing on v0.3.4; `80f9c50` records
170/300 and 60/81 respectively. All snapshot bytes match, with 180 verdict
improvements and no regressions. Runtime qualification separately establishes
74 preserving cases and seven real approximate-comparison weakenings in the
81-case population: all 81 captured before/after suites pass, but those seven
lose an independent wrong-output check. The 74 preserving cases retain it
on both sides (324 pytest runs in total). A later
precision checkpoint `247c1e6` passes all 74 preserving cases and blocks all
seven real losses. Its framework-guard follow-up retains the nine affected
passes and seven blocks; these are intermediate receipts pending a complete
integrated replay, not justification for changing historical labels.
The input is untracked and bound by its complete content hash, not by the
containing checkout's Git HEAD. The 306 unsaved capped families remain
unavailable. Private research reports have not been published or changed.

The separate frozen runtime corpora contain 60 refactors and 80 tampers.
Refactors: 57 pass strict four-run qualification; three original fixture
errors remain unqualified. Tampers: 78 pass the strict pytest XML adapter,
and `011-signum` additionally qualifies through an independent unittest run
with two completed subtest failures before and a passing test after. Its
pytest XML represents multiple failures in one testcase, outside that
adapter's one-failure-per-case model. `040-startswith` fails in teardown,
so it is not counted as a completed-test failure. These observations do not
rewrite frozen labels or old receipts.

The current frozen-engine comparison has one frozen-record mismatch:
`035-identity` changes from escape/pass to `TEST_DISABLED`/block because
removal of an inherited collected test is now visible. The original gate
continues to fail until the maintainer classifies that record update.

No tag, GitHub Release or PyPI publication is claimed by this inventory.

## Integrated pre-tag checkpoint

The complete suite on immutable snapshot `80f9c50` reports 5,187 passed,
one Windows symlink skip and three failures. Two consume the same frozen
`035-identity` mismatch; one is the missing v0.4.0 tag. The previous
keyword-helper finding-ownership regression is fixed by `02b486c`, and this
full run confirms that the original owner is retained.

Public CI on `80f9c50` records 5,188 passed and those same three failures on
all nine legs. Every performance gate passes with unchanged thresholds;
all nine raw corpus artifacts are byte-identical. Complete job logs and ten
API-hash-verified artifact ZIPs are retained. Install-from-tag explicitly
defers until the tag exists; its green status is not visitor-install evidence.
Later source changes still require fresh full qualification.

Review also reproduced production importing an apparently unused test helper:
the original suite failed twice and the edited suite passed twice. Removal of
helpers, unused fixtures and transparent forwarding definitions now requires
closed production-source proof. Factory consumers received the same
review. These checks protect against granting equivalence solely because a
definition has no references inside its own test module.

Further independent review exposed two false equivalence proofs and retained
their live failure-to-pass witnesses. A complete dictionary equality cannot
be exchanged for partial fields, including moving the complete check to a
different expected dictionary for the same input. A fixture's `request`
parameter cannot be treated as an imported callable with that name. The
repaired proof paths refuse both transformations; existing ordinary-parser
pass/warning residuals remain explicit and are not claimed newly blocked.

The following integrated source repairs additionally cover literal subject locals,
actual helper-derived parameter answers, closed fixture membership, exact
Boolean comparisons, default-class exception aliases and local arithmetic
implementations replacing the production subject. Complete capture fragments
retain every original check and multiplicity. Native table proofs now require
framework authority even without an explicit pytest import; an untrusted
local runner declines optional projection, while the ordinary-parser warning
residual remains explicit. These later repairs need fresh full qualification.
