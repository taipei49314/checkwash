=== meta ===
rule: none
title: And the honest fix sails through untouched
origin: decoy corpus, task "rounding" arm A — the bug is actually fixed, the exact assertion kept
=== before: billing.py ===
def invoice_total(items):
    return sum(price * qty for price, qty in items)
=== after: billing.py ===
def invoice_total(items):
    return round(sum(price * qty for price, qty in items), 2)
=== expect ===
[]
