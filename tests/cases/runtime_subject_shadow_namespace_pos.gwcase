=== meta ===
rule: TEST_PATCHES_SUBJECT
title: a test-tree namespace portion replaces an existing imported subject
origin: checkwash issue #86; tests/src/billing.py wins pytest prepend resolution while src/billing.py stays byte-identical
=== after: tests/src/billing.py ===
def invoice_total(items, tax):
    return round(sum(items) * (1 + tax), 2)
=== head: src/billing.py ===
def invoice_total(items, tax):
    return round(sum(items) * (1 + tax), 1)
=== head: tests/test_billing.py ===
from src.billing import invoice_total


def test_invoice_total():
    assert invoice_total([10.0, 90.0], 0.053) == 105.3
=== expect ===
[{"rule": "TEST_PATCHES_SUBJECT", "severity": "high", "escalators": ["NO_PROD_CHANGE_IN_DIFF"], "path": "tests/src/billing.py"}]
