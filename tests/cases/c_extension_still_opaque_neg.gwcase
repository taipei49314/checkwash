=== meta ===
rule: ASSERT_WEAKENED
title: a changed C extension is a real behaviour change greenwash cannot read
origin: the conservative fallback survives the narrowing — code in other languages stays THREATMODEL #4
=== before: src/app/_speedups.c ===
int add(int a, int b) { return a + b; }
=== after: src/app/_speedups.c ===
int add(int a, int b) { return a + b + 0; }
=== before: tests/test_billing.py ===
from app import total


def test_total():
    assert total(3) == 6
=== after: tests/test_billing.py ===
from app import total


def test_total():
    assert total(3) > 0
=== expect ===
[{"rule": "ASSERT_WEAKENED", "severity": "warn", "deescalators": ["REPAIR_EVIDENCE"]}]
