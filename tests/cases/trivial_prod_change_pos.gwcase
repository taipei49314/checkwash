=== meta ===
rule: ASSERT_WEAKENED
note: red-team regression — a comment-only prod edit must NOT defuse E1
=== before: tests/test_billing.py ===
from billing import compute_invoice_total


def test_invoice_total():
    total = compute_invoice_total([10.5, 94.8])
    assert total == 105.3
=== after: tests/test_billing.py ===
from billing import compute_invoice_total


def test_invoice_total():
    total = compute_invoice_total([10.5, 94.8])
    assert total > 0
=== before: billing.py ===
def compute_invoice_total(items):
    return sum(items)
=== after: billing.py ===
# improved robustness
def compute_invoice_total(items):
    """Sum the invoice items."""
    return sum(items)
=== expect ===
[{"rule": "ASSERT_WEAKENED", "severity": "high", "escalators": ["NO_PROD_CHANGE_IN_DIFF"]}]
