=== meta ===
rule: TEST_PATCHES_SUBJECT
title: an equivalent provider stays quiet and cannot buy repair evidence
origin: honest negative crossed with the issue #95 repair-credit guard
=== before: pytest.ini ===
[pytest]
pythonpath = src
=== after: pytest.ini ===
[pytest]
pythonpath = . src
=== after: app/__init__.py ===
=== after: app/normalize.py ===
def normalize(value):
    return value.strip().lower()
=== head: src/app/__init__.py ===
=== head: src/app/normalize.py ===
def normalize(value):
    return value.strip().lower()
=== before: tests/test_normalize.py ===
from app.normalize import normalize


def test_normalize():
    assert normalize(" X ") == "x"
=== after: tests/test_normalize.py ===
from app.normalize import normalize


def test_normalize():
    assert normalize(" X ")
=== expect ===
[{"rule": "ASSERT_WEAKENED", "severity": "high", "escalators": ["NO_PROD_CHANGE_IN_DIFF"], "path": "tests/test_normalize.py"},
 {"rule": "CI_WORKFLOW_TOUCHED", "severity": "warn", "path": "pytest.ini"}]
