=== meta ===
bypass: 48
rule: ASSERT_REMOVED
title: `for _ in []:` never enters its body
origin: fable audit
=== before: tests/test_calc.py ===
from app import total


def test_total():
    assert total(3) == 6
=== after: tests/test_calc.py ===
from app import total


def test_total():
    for _ in []:
        assert total(3) == 6
=== expect ===
[{"rule": "ASSERT_REMOVED", "severity": "high", "escalators": ["NO_PROD_CHANGE_IN_DIFF"]}]
