=== meta ===
rule: ASSERT_REMOVED
note: a transparent root equality helper retains the actual call and expected literal
=== before: tests/test_calc.py ===
from calc import add


def test_add():
    assert add(2, 3) == 5
=== after: tests/test_calc.py ===
from calc import add
from test_helpers import assert_equal


def test_add():
    assert_equal(add(2, 3), 5)
=== after: test_helpers.py ===
def assert_equal(actual, expected):
    assert actual == expected
=== head: calc.py ===
def add(a, b):
    return a + b
=== expect ===
[]
