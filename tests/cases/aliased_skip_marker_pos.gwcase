=== meta ===
bypass: 32
rule: TEST_DISABLED
title: `import pytest as p; @p.mark.skip` is the same marker
origin: reader audit — matching the literal dotted string missed the alias
=== before: tests/test_calc.py ===
from app import calc


def test_calc():
    assert calc() == 3
=== after: tests/test_calc.py ===
import pytest as p

from app import calc


@p.mark.skip
def test_calc():
    assert calc() == 3
=== expect ===
[{"rule": "TEST_DISABLED", "severity": "high", "escalators": ["NO_PROD_CHANGE_IN_DIFF"]}]
