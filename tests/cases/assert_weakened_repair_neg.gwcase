=== meta ===
rule: ASSERT_WEAKENED
note: legitimate behaviour change — the called prod symbol really changed (D1)
=== before: tests/test_billing.py ===
from billing import compute_invoice_total


def test_invoice_total():
    total = compute_invoice_total([10.5, 94.8])
    assert total == 105.3
=== after: tests/test_billing.py ===
from billing import compute_invoice_total


def test_invoice_total():
    total = compute_invoice_total([10.5, 94.8])
    assert total == 110.57
=== before: billing.py ===
def compute_invoice_total(items):
    return sum(items)
=== after: billing.py ===
TAX = 1.05


def compute_invoice_total(items):
    return sum(items) * TAX
=== expect ===
[{"rule": "EXPECTED_VALUE_CHANGED", "severity": "warn", "deescalators": ["REPAIR_EVIDENCE"]}]
