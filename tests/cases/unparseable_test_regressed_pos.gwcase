=== meta ===
rule: TEST_FILE_UNPARSEABLE
title: A test file that stops parsing has left greenwash's reach
origin: fable audit — silent skip made the verdict interpreter-version dependent
=== before: tests/test_calc.py ===
from app import total


def test_total():
    assert total(3) == 6
=== after: tests/test_calc.py ===
from app import total


def test_total(:
    assert total(3) == 6
=== expect ===
[{"rule": "TEST_FILE_UNPARSEABLE", "severity": "high", "escalators": ["TEST_BECAME_UNANALYSABLE"]},
 {"rule": "TEST_DISABLED", "severity": "high"}]
