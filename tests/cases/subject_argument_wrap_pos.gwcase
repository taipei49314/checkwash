=== meta ===
bypass: 84
rule: SUBJECT_NORMALIZED
title: The argument is wrapped, not the subject
origin: redteam-weaknesses.md §4B — containment looked at the whole call and saw no overlap
=== before: tests/test_encode.py ===
from app import encode_path


def test_non_ascii():
    s = "caf\u00e9"
    assert encode_path(s) == "caf%C3%A9"
=== after: tests/test_encode.py ===
from app import encode_path, normalise


def test_non_ascii():
    s = "caf\u00e9"
    assert encode_path(normalise(s)) == "caf%C3%A9"
=== expect ===
[{"rule": "SUBJECT_NORMALIZED", "severity": "high", "escalators": ["NO_PROD_CHANGE_IN_DIFF"]}]
