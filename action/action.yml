name: checkwash
description: >-
  Deterministic, zero-LLM detector for PR diffs that tamper with the test
  oracle. Blocks on high-severity findings; every finding carries verbatim
  before/after evidence.
branding:
  icon: shield
  color: green

inputs:
  fail-on:
    description: severity that blocks (info|warn|high|critical)
    required: false
    default: high
  base:
    description: base ref to diff against (defaults to the PR base)
    required: false
    default: ""
  comment-pr:
    description: >-
      If true, post one PR review comment per high/critical finding.
      Requires pull-requests: write. Soft-fails if the token cannot write.
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - id: install
      shell: bash
      run: python -m pip install --quiet "${{ github.action_path }}/.."
    - id: check
      shell: bash
      env:
        PYTHONUTF8: "1"
        CHECKWASH_BASE: ${{ inputs.base }}
        CHECKWASH_FAIL_ON: ${{ inputs.fail-on }}
        CHECKWASH_PR_BASE: ${{ github.event.pull_request.base.sha }}
      run: |
        BASE="$CHECKWASH_BASE"
        if [ -z "$BASE" ] && [ -n "$CHECKWASH_PR_BASE" ]; then
          BASE="$CHECKWASH_PR_BASE"
        fi
        if [ -z "$BASE" ]; then
          BASE="HEAD~1"
        fi
        git fetch --quiet --deepen=200 origin || true
        set +e
        checkwash check "$BASE...HEAD" --fail-on "$CHECKWASH_FAIL_ON" --format term | tee "$GITHUB_STEP_SUMMARY"
        code=${PIPESTATUS[0]}
        echo "base=$BASE" >> "$GITHUB_OUTPUT"
        echo "fail_on=$CHECKWASH_FAIL_ON" >> "$GITHUB_OUTPUT"
        echo "exit_code=$code" >> "$GITHUB_OUTPUT"
        set -e
        exit $code
    - id: comment
      if: ${{ inputs.comment-pr == 'true' }}
      shell: bash
      env:
        PYTHONUTF8: "1"
        CHECKWASH_BASE: ${{ steps.check.outputs.base }}
        CHECKWASH_FAIL_ON: ${{ steps.check.outputs.fail_on }}
        GH_TOKEN: ${{ github.token }}
        GH_REPO: ${{ github.repository }}
        PR_NUMBER: ${{ github.event.pull_request.number }}
      run: |
        set +e
        if [ -z "$PR_NUMBER" ] || [ -z "$GH_TOKEN" ]; then
          echo "comment-pr skipped: not a pull_request or no token"
          exit 0
        fi
        tmp="$(mktemp)"
        checkwash check "$CHECKWASH_BASE...HEAD" --fail-on "$CHECKWASH_FAIL_ON" --format json > "$tmp"
        python "${{ github.action_path }}/post_review.py" "$tmp"
        status=$?
        rm -f "$tmp"
        if [ "$status" != "0" ]; then
          echo "comment-pr soft-failed (exit $status); the check verdict above still stands"
        fi
        exit 0
